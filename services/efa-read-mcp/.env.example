# Required. Inject at process start from an approved secret mechanism.
DATABASE_URL=

# Optional bounded runtime settings.
EFA_MCP_POOL_MIN_SIZE=1
EFA_MCP_POOL_MAX_SIZE=4
EFA_MCP_STATEMENT_TIMEOUT_MS=10000
EFA_MCP_LOCK_TIMEOUT_MS=3000
